---
description: Completed customer workshop jobs with charges and no invoice. The write-off queue if nobody acts.
---

Run `node scripts/dealer.mjs unbilled --json`. For each: `invoice build RO-xx` is the one-line fix. Recon never appears here; it lands in the unit's cost.
