---
description: The parts shelf - what is out or under its reorder point, and receiving stock in.
---

1. Run `node scripts/dealer.mjs parts --json` (alerts only; `--all` for the whole shelf).
2. OUT before low. Receiving: `parts receive CODE --qty=`.
