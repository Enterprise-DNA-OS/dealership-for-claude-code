---
description: The staff - who holds a WoF inspector authorisation and when it expires, who holds WoF bookings, open deals and deliveries per salesperson.
---

Run `node scripts/dealer.mjs team --json`. Call out loudly any technician whose inspector authorisation is EXPIRED or expiring while they still hold WoF bookings: that work gets reassigned today (`ro assign RO-xx --tech=`).
